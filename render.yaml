services:
  - type: web
    name: duck-hunt
    runtime: python
    rootDir: DuckHunt-JS-master/backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
